{
  "compilerOptions": {
    // …existing options…
    "jsx": "react-jsx",             // for React 17+ automatic JSX runtime
    "skipLibCheck": true,           // skip third-party type checks
    "noUnusedLocals": false,        // allow imports you haven’t yet wired up
    "noImplicitAny": false          // temporarily allow parameters without explicit types
  }
}
