{
  "compilerOptions": {
    // enable default-import from CommonJS modules
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    // use the new JSX transform (no need to import React)
    "jsx": "react-jsx",

    // keep these from our earlier tweaks
    "skipLibCheck": true,
    "noUnusedLocals": false,
    "noImplicitAny": false
  }
}
