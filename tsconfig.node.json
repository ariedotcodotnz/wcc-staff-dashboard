{
  "compilerOptions": {
    // This setting is required for project references.
    "composite": true,

    // Keep these settings for a modern Node.js environment
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "skipLibCheck": true,

    // This is the fix for error TS6310. We REMOVE "noEmit": true.
    // The other linting rules are good to keep.
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    // It's also good practice to allow this in a node config
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}